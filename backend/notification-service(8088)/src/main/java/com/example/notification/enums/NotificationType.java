package com.example.notification.enums;

public enum NotificationType {
    BOOKING_REQUEST,      // Driver receives when passenger books their ride
    BOOKING_ACCEPTED,     // Passenger receives when driver accepts booking
    BOOKING_REJECTED      // Passenger receives when driver rejects booking
}
